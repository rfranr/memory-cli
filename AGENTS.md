# INSTRUCTIONS
RAG-CLI is a command line tool that
    - embed a list of 'categories and taxonomy' system using POST /embedding
        - index document command 
    - store this embeddings in a local database adding the metada for the 'categories and taxonomy'
        - persist in local database 
    - embed a text , and compare witrh local database, Returns occurrencies sorted from most nearest to fastest with metada  
# TECHNICAL DEPENDENCIES
    sqlite or sqlite-vec
    typescript bundled 


# REQUERIMENTS
    inspect ./src folder for comments

    
